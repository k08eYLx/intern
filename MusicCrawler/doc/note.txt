歌手 ===> 听众数
专辑 ===> 播放次数
歌曲 ===> 热度值/播放次数

TODO:
	将计算汉字排序码合并到爬去信息的过程中，用ext tables替代原来的表。
	统一配置文件
	为专辑增加播放次数字段

2013-3-9：
	对于MySQL数据库统一使用Insert on duplicate key update进行更新
	对于H2数据库统一使用Merge into进行更新
	
2013-3-1：
	爬取数据更全面（main-url，other-male，other-female，other-group）

2013-2-28：
	移除了外键约束部分
	歌手类别改由歌手页面上的类别确定（原来是初始化的线程名）

2013-1-20:
	增加对H2的支持，可以使用H2替代MySQL，并将H2作为默认数据库。
	增加了数据库的配置文件；增加了建表的过程；改进了对代理的判断。

去除了用于计数出现输入验证码页面的count变量和AccessDeniedException异常。

歌手表添加听众数字段。



打包后，输出中文乱码。

网络连接还是需要设置超时的，jsoup默认connect和read各为3s，如果timeout设置为0，表示无限，可能导致一直阻塞。

加入增量抓取，获取数据前判断，如果已存在就不再获取。

歌手名、歌曲名、专辑名 都可能含有单引号
专辑发行时间可能是“时间未知”
歌曲发行时间可能是不符合“dddd-dd-dd”的形式，如“dddd年”
